/**
 * postcss.config.js
 * 
 * PostCSS configuration for CSS processing.
 * 
 * Plugins:
 * - tailwindcss: Generates utility-first CSS classes
 * - autoprefixer: Adds vendor prefixes for browser compatibility
 */

export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
